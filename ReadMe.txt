precondition

# Install nodejs - go to https://nodejs.org/en/ download windows version and install it.
# Run install.bat
# Run phantom.bat, then the phantom will listen port 4444.
# Run run.bat. The task will start. One time per 24 hours.

